language: python

python:
  - 3.6
  - 3.5
  - pypy3

install:
  - pip install pipenv -i https://mirrors.aliyun.com/pypi/simple/ 
  - pipenv install --skip-lock -d

script:
  - make cov

after_success:
  - codecov
